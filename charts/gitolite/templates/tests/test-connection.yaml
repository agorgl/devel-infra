apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitolite.fullname" . }}-test-connection"
  labels:
    {{- include "gitolite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitolite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
